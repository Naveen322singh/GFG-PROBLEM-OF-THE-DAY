class Solution
{
    public void swapElements(int[] arr, int n)
    {
        // Code here
        for(int i=0;i<n-2;i++)
        {
            int j=i+2;
            int temp=arr[i];
            arr[i]=arr[j];
            arr[j]=temp;
        }
    }
}
